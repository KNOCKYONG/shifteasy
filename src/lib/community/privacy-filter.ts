/**
 * 커뮤니티 정식 론칭 전까지 사용할
 * 매우 단순한 텍스트 필터입니다.
 *
 * - 현재는 입력 값을 그대로 돌려주며,
 *   향후 욕설/개인정보 필터링 로직을 이곳에 추가하면 됩니다.
 */
export function filterSensitiveContent(input: string): string {
  return input;
}

